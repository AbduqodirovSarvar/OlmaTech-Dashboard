import { TranslateDataPipe } from './translate-data.pipe';

describe('TranslateDataPipe', () => {
  it('create an instance', () => {
    const pipe = new TranslateDataPipe();
    expect(pipe).toBeTruthy();
  });
});
